apply plugin: 'com.android.library'
apply plugin: 'kotlin-android'

group = 'expo.modules.healthbridge'
version = '0.1.0'

def expoModulesCorePlugin = new File(project(":expo-modules-core").projectDir.absolutePath, "ExpoModulesCorePlugin.gradle")
apply from: expoModulesCorePlugin
applyKotlinExpoModulesCorePlugin()
useCoreDependencies()

android {
    namespace "expo.modules.healthbridge"
    compileSdkVersion rootProject.ext.has('compileSdkVersion') ? rootProject.ext.compileSdkVersion : 35

    defaultConfig {
        minSdkVersion rootProject.ext.has('minSdkVersion') ? rootProject.ext.minSdkVersion : 26
        targetSdkVersion rootProject.ext.has('targetSdkVersion') ? rootProject.ext.targetSdkVersion : 35
    }

    buildTypes {
        release {
            minifyEnabled false
        }
    }

    lintOptions {
        abortOnError false
    }
}

dependencies {
    // Health Connect min SDK 26 gerektirir (bkz. defaultConfig.minSdkVersion override) —
    // uygulamanın genel minSdkVersion'ı (24, bkz. wear-bridge) bu modül için yeterli değil.
    implementation "androidx.health.connect:connect-client:1.1.0-rc03"
    implementation "androidx.activity:activity-ktx:1.9.0"
}
